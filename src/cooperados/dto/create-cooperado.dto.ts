import { IsEmail, IsEnum, IsNotEmpty, IsString, MinLength, Matches } from 'class-validator';
import { CreateUserDto } from 'src/auth/dto/create-auth.dto';

  
export class CreateCooperadoDto extends CreateUserDto {
  
    
}
